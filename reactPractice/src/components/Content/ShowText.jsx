
function ShowText({ text, classes }) {

    return (
        <>
        <div className={ classes }>
            <h4>{ text }</h4>
        </div>
            
        </>
    );
}

export default ShowText;